# 🚀 Render 部署指南 - 火柴人游戏

## 📋 目录
- [快速部署](#快速部署)
- [配置说明](#配置说明)
- [问题排查](#问题排查)
- [验证部署](#验证部署)

---

## ⚡ 快速部署（3分钟完成）

### 方法1：使用 Render 蓝图（推荐）

1. **登录 Render Dashboard**
   - 访问: https://dashboard.render.com
   - 使用 GitHub 账号登录

2. **新建蓝图**
   - 点击 "New" → "Blueprint"
   - 选择 "Connect GitHub"
   - 选择 `hbpc002/stickman-fighter` 仓库
   - 选择 `render.yaml` 文件
   - 点击 "Apply"

3. **等待部署**
   - Render 会自动配置并部署
   - 大约 1-2 分钟完成
   - 部署完成后会显示 URL

### 方法2：手动配置 Web Service

1. **新建 Web Service**
   - 点击 "New" → "Web Service"
   - 选择 GitHub 仓库
   - 选择 `stickman-fighter` 仓库

2. **配置服务**
   ```
   Service Name: stickman-fighter
   Environment: Python
   Build Command: pip install -r requirements.txt
   Start Command: python app.py
   Instance Type: Free (免费)
   ```

3. **添加环境变量**
   ```
   PORT = 10000
   ```

4. **部署**
   - 点击 "Create Web Service"
   - 等待构建和部署完成

---

## 🔧 配置说明

### 必需文件

#### 1. requirements.txt
```txt
flask==3.0.0
gunicorn==21.2.0  # 可选，用于生产环境
```

#### 2. Procfile（可选，Heroku兼容）
```txt
web: python app.py
```

#### 3. app.py
- 必须使用 `os.environ.get('PORT', 5000)` 读取端口
- 必须绑定到 `0.0.0.0`
- 必须提供 `/api/health` 端点

#### 4. render.yaml（蓝图配置）
- 自动配置所有参数
- 推荐使用此方法

### Render 环境变量

在 Render Dashboard → Settings → Environment Variables:

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `PORT` | `10000` | Render 分配的端口 |
| `PYTHON_VERSION` | `3.11.2` | Python 版本（可选）|

---

## 🐛 问题排查

### 问题1：页面白屏，无游戏画面

#### 可能原因：
1. **代码未更新** - Render 使用旧版本
2. **构建失败** - 依赖安装失败
3. **启动命令错误** - 未正确运行 app.py

#### 解决方案：

**步骤1：检查部署的代码版本**
```bash
# 在 Render Logs 中查看
# 应该看到最新的提交哈希: 36afdb1
```

**步骤2：强制重新部署**
```
Dashboard → stickman-fighter → Manual Deploy → Deploy latest commit
```

**步骤3：清除构建缓存**
```
Dashboard → stickman-fighter → Clear Build Cache → Deploy
```

### 问题2：按钮点击无响应

#### 可能原因：
1. **JavaScript 错误** - 控制台有异常
2. **事件监听器未绑定** - 初始化失败
3. **Canvas 未准备好** - 游戏循环未启动

#### 解决方案：

**检查浏览器控制台**
```javascript
// 按 F12 打开开发者工具
// 查看 Console 标签
// 应该看到:
✅ Canvas准备就绪
✅ 游戏初始化完成
🎮 游戏加载完成！按 R 重新开始
```

**如果看到错误：**
- `Canvas未找到` → 检查 HTML 是否正确渲染
- `initAudio is not defined` → 代码版本过旧

### 问题3：API 返回 404 或 500 错误

#### 检查健康检查端点
```
访问: https://stickman-fighter-h1fy.onrender.com/api/health
```

**正常响应：**
```json
{
  "status": "healthy",
  "service": "stickman-fighter-v2.6-weapon",
  "version": "2.6"
}
```

**如果返回 404/500：**
- 检查 Render Logs 中的 Python 错误
- 确认 Flask 路由正确配置

### 问题4：混合内容错误 (Mixed Content)

#### 症状：
- 浏览器控制台显示 "Mixed Content" 警告
- HTTPS 页面加载 HTTP 资源被阻止

#### 解决方案：
**检查 app.py 中的代码**
```python
# 确保没有硬编码的 http:// 链接
# 所有资源使用相对路径或 HTTPS

# ❌ 错误示例
canvas.style.backgroundImage = "url(http://example.com/bg.jpg)";

# ✅ 正确示例
canvas.style.backgroundImage = "url(/static/bg.jpg)";  // 相对路径
```

---

## ✅ 验证部署成功

### 测试清单

#### 1. 基础功能测试
```bash
# 访问主页面
https://stickman-fighter-h1fy.onrender.com/

# 应该看到：
- ✅ 蓝色渐变背景
- ✅ 左右控制面板
- ✅ 底部6个蓝色按钮
- ✅ 中间Canvas区域
```

#### 2. API 健康检查
```bash
curl https://stickman-fighter-h1fy.onrender.com/api/health

# 应该返回：
{"status":"healthy","service":"stickman-fighter-v2.6-weapon","version":"2.6"}
```

#### 3. 浏览器控制台检查
```javascript
// 按 F12 → Console
// 应该看到：
🎮 游戏初始化开始...
✅ Canvas准备就绪
✅ Canvas尺寸: 800 x 500
🔄 游戏重置完成
🎉 游戏加载完成！按 R 重新开始
```

#### 4. 功能测试
- [ ] 点击"🔄 重置" → 显示两个玩家
- [ ] 点击"⏸️ 暂停" → 游戏暂停
- [ ] 点击"🖥️ 全屏" → 进入全屏
- [ ] 点击"🤖 AI" → AI模式开启
- [ ] 点击"💀 硬核" → 硬核模式开启
- [ ] 点击"⚔️ 武器" → 显示武器说明
- [ ] 键盘 WASD → 玩家1移动
- [ ] 键盘 方向键 → 玩家2移动
- [ ] 键盘 F/G → 玩家1攻击
- [ ] 键盘 J/K → 玩家2攻击

---

## 🛠️ Render Dashboard 操作指南

### 查看部署日志
```
Dashboard → stickman-fighter → Events
```

**正常日志示例：**
```
Build started
Collecting flask==3.0.0
Successfully installed flask-3.0.0
🚀 启动服务器: http://localhost:10000
🔥 火柴人对战 - V2.6 创意武器系统版
```

**错误日志示例：**
```
ERROR: Could not find a version that satisfies flask==3.0.0
→ requirements.txt 格式错误
```

### 查看实时日志
```
Dashboard → stickman-fighter → Logs
```

### 重新部署
```
Dashboard → stickman-fighter → Manual Deploy
→ 选择 "Deploy latest commit"
```

### 清除缓存
```
Dashboard → stickman-fighter → Clear Build Cache
→ 然后重新部署
```

### 修改配置
```
Dashboard → stickman-fighter → Settings
→ 修改 Build/Start Command
→ 点击 "Save Changes"
→ 触发重新部署
```

---

## 📊 Render 部署配置对比表

| 配置项 | 推荐值 | 常见错误 | 后果 |
|--------|--------|----------|------|
| **Service Type** | Python Web Service | Static Site | 静态站点无法运行 Flask |
| **Build Command** | `pip install -r requirements.txt` | `pip install flask` | 可能缺少依赖 |
| **Start Command** | `python app.py` | `flask run` | 忽略 PORT 环境变量 |
| **Port** | `10000` | `5000` | Render 可能不接受 |
| **Python Version** | `3.11.2` | `3.12` | 某些包可能不兼容 |

---

## 🎯 常见问题快速解决

### 问题：Render 部署后页面 404

**快速修复：**
1. 检查 Start Command 是否为 `python app.py`
2. 确认 app.py 中有 `@app.route('/')`
3. 查看 Logs 是否有启动错误

### 问题：按钮点击无反应

**快速修复：**
1. 清除浏览器缓存 (Ctrl+Shift+R)
2. 检查浏览器控制台是否有错误
3. 确认部署的是最新代码

### 问题：游戏画面不显示

**快速修复：**
1. 检查 `/api/health` 是否返回正常
2. 查看 Render Logs 是否有 Python 错误
3. 强制重新部署最新代码

---

## 📞 获取帮助

### 需要提供的信息

如果问题无法解决，请提供：

1. **Render 服务配置截图**
   - Settings 页面的所有配置

2. **构建日志**
   - Events 页面的完整日志

3. **浏览器控制台截图**
   - F12 → Console 的错误信息

4. **网络请求截图**
   - F12 → Network 的请求状态

5. **访问的完整 URL**
   - 例如: `https://stickman-fighter-h1fy.onrender.com/`

### 联系方式

- **GitHub Issues**: https://github.com/hbpc002/stickman-fighter/issues
- **Render 支持**: https://render.com/support

---

## 🎉 部署成功后的建议

### 1. 启用自动部署
```
Settings → Auto Deploy → ON
```

### 2. 设置自定义域名
```
Settings → Custom Domains → Add Domain
```

### 3. 监控使用情况
```
Dashboard → 查看带宽、请求次数
```

### 4. 备份配置
```
下载 render.yaml 和所有配置文件
```

---

**最后更新**: 2025-12-27
**版本**: V2.6.1
**状态**: ✅ 已验证