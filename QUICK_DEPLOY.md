# 🚀 火柴人对战游戏 - 5分钟快速部署指南

## 🎯 选择你的部署方式

### 方式1: 本地运行 (最快) ⭐
```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 启动游戏
python3 app.py

# 3. 浏览器访问
# 打开: http://localhost:5000
```

### 方式2: Docker (推荐) 🐳
```bash
# 1. 启动服务
docker-compose up -d --build

# 2. 查看状态
docker-compose ps

# 3. 访问游戏
# 打开: http://localhost:5000

# 4. 停止服务
docker-compose down
```

### 方式3: 使用启动脚本 🎮
```bash
# 给予执行权限
chmod +x start.sh

# 运行启动器
./start.sh

# 选择: 1 (标准版) 或 2 (增强版)
```

---

## ☁️ 云平台部署 (5分钟)

### 选项A: Heroku (最简单)
```bash
# 1. 安装 Heroku CLI
# 访问: https://devcenter.heroku.com/articles/heroku-cli

# 2. 登录
heroku login

# 3. 创建应用
heroku create your-game-name

# 4. 部署
git push heroku main

# 5. 打开游戏
heroku open
```

### 选项B: Railway (现代化)
1. 访问 [railway.app](https://railway.app)
2. 使用 GitHub 登录
3. 点击 "New Project" → "Deploy from GitHub"
4. 选择仓库 `hbpc002/stickman-fighter`
5. 等待自动部署完成

### 选项C: Render (免费额度多)
1. 访问 [render.com](https://render.com)
2. 创建 Web Service
3. 连接 GitHub 仓库
4. 配置:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python app.py`
   - Port: `5000`
5. 点击部署

### 选项D: Fly.io (全球网络)
```bash
# 1. 安装 flyctl
curl -L https://fly.io/install.sh | sh

# 2. 登录
fly auth login

# 3. 部署
fly launch
fly deploy
```

---

## 🎮 游戏测试

### 启动后验证
```bash
# 检查服务状态
curl http://localhost:5000/api/health

# 应该返回:
# {"status":"healthy","service":"stickman-fighter"}
```

### 浏览器访问
打开浏览器访问: `http://localhost:5000`

**控制说明:**
- 玩家1: WASD移动, F=拳, G=踢腿
- 玩家2: 方向键移动, J=拳, K=踢腿
- ESC: 暂停, R: 重新开始

---

## 🔧 常见问题

### Q: 端口被占用?
```bash
# 使用不同端口
PORT=8080 python3 app.py
```

### Q: 依赖安装失败?
```bash
# 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q: Docker 构建失败?
```bash
# 清理缓存
docker system prune -a
docker-compose build --no-cache
```

### Q: 想要增强版功能?
```bash
# 运行增强版
python3 app_enhanced.py

# 包含: 连击系统、特殊技能、AI对战、音效等
```

---

## 📊 部署检查清单

- [ ] 选择部署平台
- [ ] 准备 GitHub 仓库 (如需要)
- [ ] 配置环境变量 (PORT)
- [ ] 测试本地运行
- [ ] 执行部署
- [ ] 验证访问
- [ ] 配置自定义域名 (可选)

---

## 🎯 推荐方案

| 场景 | 推荐方案 | 原因 |
|------|----------|------|
| 快速测试 | 本地运行 | 零配置，即时反馈 |
| 个人项目 | Railway | 现代化，免费额度足 |
| 长期运行 | Render | 稳定，750小时/月 |
| 生产环境 | Fly.io | 全球加速，性能好 |
| 学习目的 | Docker | 学习容器化技术 |

---

## 📞 获取帮助

- **详细文档**: `DEPLOYMENT_GUIDE.md`
- **项目主页**: https://github.com/hbpc002/stickman-fighter
- **快速指南**: `快速开始.md`

---

## ✅ 成功标志

部署成功后，你应该看到:
1. 🎮 游戏界面正常显示
2. ⌨️ 键盘控制响应正常
3. 🎯 生命值和体力条正常工作
4. 🏆 可以正常对战和获胜

---

**祝部署顺利！🎮**

如需帮助，请查看详细文档或联系项目维护者。
