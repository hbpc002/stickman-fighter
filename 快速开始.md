# 🚀 火柴人对战游戏 - 快速开始指南

## 📁 项目文件

所有文件已准备就绪：

```
/home/hbpc/
├── app.py                    # Web版本游戏 (Flask)
├── stickman_fighter.py       # 桌面版本游戏 (Pygame)
├── requirements.txt          # Python依赖
├── Dockerfile               # Docker配置
├── docker-compose.yml       # Docker编排
├── deploy.sh                # 一键部署脚本
├── Procfile                 # Heroku配置
├── VERCEL.json              # Vercel配置
├── README.md                # 项目说明
├── DEPLOYMENT_GUIDE.md      # 详细部署指南
└── 快速开始.md              # 本文件
```

---

## 🎯 3种快速部署方式

### 方式1: 本地测试 (最简单)

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 运行游戏
python app.py

# 3. 浏览器访问
# 打开: http://localhost:5000
```

### 方式2: Docker 部署 (推荐)

```bash
# 1. 确保 Docker 已安装
docker --version

# 2. 一键启动
./deploy.sh
# 选择: 1 (本地 Docker 部署)

# 3. 访问游戏
# 打开: http://localhost:5000
```

### 方式3: 云平台部署 (生产环境)

```bash
# 运行部署助手
./deploy.sh

# 选择部署方式:
# 2 - Heroku (最简单)
# 3 - Railway (现代)
# 4 - Render (免费额度多)
# 5 - Fly.io (全球网络)
```

---

## 🎮 游戏控制

**玩家1 (红色):**
- `W` - 跳跃
- `A` - 左移
- `D` - 右移
- `F` - 出拳
- `G` - 踢腿

**玩家2 (蓝色):**
- `↑` - 跳跃
- `←` - 左移
- `→` - 右移
- `J` - 出拳
- `K` - 踢腿

**其他:**
- `R` - 重新开始
- `ESC` - 暂停

---

## 📊 部署平台对比

| 平台 | 难度 | 费用 | 适合场景 |
|------|------|------|----------|
| **本地运行** | ⭐ | 免费 | 快速测试 |
| **Docker** | ⭐⭐ | 免费 | 本地/服务器 |
| **Heroku** | ⭐⭐ | 免费 | 个人项目 |
| **Railway** | ⭐⭐ | 免费/付费 | 现代应用 |
| **Render** | ⭐⭐ | 免费750h/月 | 长期运行 |
| **Fly.io** | ⭐⭐⭐ | 免费/付费 | 全球部署 |
| **自托管** | ⭐⭐⭐⭐ | 服务器费用 | 完全控制 |

---

## 🔧 常用命令

### Docker 操作
```bash
# 启动
docker-compose up -d

# 停止
docker-compose down

# 查看日志
docker-compose logs -f

# 重启
docker-compose restart
```

### Git 操作 (云平台部署)
```bash
# 初始化仓库
git init
git add .
git commit -m "Initial commit"

# 推送到 GitHub
git remote add origin https://github.com/your-username/your-repo.git
git push -u origin main
```

---

## 🛠️ 故障排除

### 问题: 端口被占用
```bash
# 修改端口
PORT=8080 python app.py
```

### 问题: 依赖安装失败
```bash
# 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 问题: Docker 构建失败
```bash
# 清理缓存
docker system prune -a
docker-compose build --no-cache
```

---

## 📞 获取帮助

详细部署指南: 查看 `DEPLOYMENT_GUIDE.md`
项目说明: 查看 `README.md`

---

## ✅ 推荐流程

1. **先本地测试**: `python app.py`
2. **再用 Docker**: `./deploy.sh` → 选择 1
3. **最后上云**: `./deploy.sh` → 选择 2-7

**祝你部署顺利！🎮**
